name: 🧪 Artifact Consumer Workflow

on:
  workflow_run:
    workflows: ["🔬 Artifact Producer Workflow"]
    types:
      - completed

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download the artifact dynamically
        uses: actions/download-artifact@v4
        with:
          name: ${{ github.event.workflow_run.output.artifact_name }}
