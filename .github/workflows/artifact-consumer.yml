name: 🧪 Artifact Consumer

on:
  workflow_run:
    workflows: ["🔬 Artifact Producer"]
    types:
      - completed

jobs:
  download_artifact:
    name: Download Artifact produced by Producer
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact from producer
        uses: actions/download-artifact@v4
        with:
          name: dynamic-artifact-${{ github.run_id }}  # Dynamically use the run_id
